network:
    version: 2
    renderer: networkd
    wifis:
        wlan0:
            dhcp4: true
            access-points:
                "{{ wifi_ssid }}":
                    password: "{{ wifi_psk }}"
